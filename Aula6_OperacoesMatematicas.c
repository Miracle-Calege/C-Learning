#include <stdio.h>
#include <math.h>//biblioteca que contem as funcoes matematicas
#include <stdlib.h>

int main() {

    int x=2,y=-3;
    float pi=3.14f;
    //x=sqrt(x);//raiz quadrada
       // x=pow(x,4);//potencia x^4
    //pi=round(pi);//arredondamento normal
    //pi=ceil(pi);//arredonamento para cima
    //pi=floor(pi);//arredondamento para baixo
    //y=abs(y);//valor absoluto
    //pi=log(pi);//logaritmo natural
    //pi=sin(pi);//seno
    //pi=cos(pi);//cosseno
    //pi=tan(pi);//tangente
     printf("%f",pi);




    return 0;
}
