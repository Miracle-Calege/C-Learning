#include <stdio.h> //usamos sempre para ter acesso a biblioteca principal do c

int main() {//o programa so corre tem a funcao main

   printf("EStou a programar em C\n");//usamos para imprimir
   //usamos "\n" para mudar de linha
    printf("It's awesome");

    return 0;//sempre finalizamos a funcao com esse retorno
}

